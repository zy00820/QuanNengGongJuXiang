<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 仅支持安卓 10+ (API 29+)，所有 AndroidX 库 minSdk 均已满足，无需 overrideLibrary -->

    <!-- 离线工具：仅本地权限，无网络/后台/隐私收集 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 安卓 13 (API 33) 以下读取外部存储仍需此权限（API 33+ 已用细粒度 READ_MEDIA_*） -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- 安卓 13+ 通知权限（运行时申请） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 检查更新需要联网（仅用户主动触发） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 电池信息 -->
    <uses-permission android:name="android.permission.BATTERY_STATS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.QNGXJ"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.toolbox.AppListActivity"
            android:exported="false"
            android:label="@string/title_app_list" />
        <activity
            android:name=".ui.toolbox.AppDetailActivity"
            android:exported="false"
            android:label="@string/title_app_detail" />
        <activity
            android:name=".ui.toolbox.VendorToolsActivity"
            android:exported="false"
            android:label="@string/title_vendor" />
        <activity
            android:name=".ui.member.MemberActivity"
            android:exported="false"
            android:label="@string/title_member" />
        <activity
            android:name=".ui.update.UpdateActivity"
            android:exported="false"
            android:label="@string/title_update" />
        <activity
            android:name=".ui.shizuku.ShizukuManagerActivity"
            android:exported="false"
            android:label="@string/title_shizuku_manager" />
        <activity
            android:name=".ui.mine.AboutActivity"
            android:exported="false"
            android:label="@string/title_about" />
        <activity
            android:name=".ui.mine.NotifySettingsActivity"
            android:exported="false"
            android:label="@string/title_notify" />
        <activity
            android:name=".ui.firmware.FirmwareDetailActivity"
            android:exported="false"
            android:label="@string/title_firmware_detail" />

        <!-- Shizuku provider：Shizuku SDK 要求宿主 App 手动声明，
             authority 必须为 ${applicationId}.shizuku，用于绑定已运行的 Shizuku 服务 -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- FileProvider：用于将下载的 APK 文件共享给系统安装器 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
